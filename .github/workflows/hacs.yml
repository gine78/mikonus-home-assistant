name: HACS validation
on:
  push:
  pull_request:
  workflow_dispatch:
permissions: {}
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: hacs/action@main
        with:
          category: integration
          # Custom-repository beta: no default-list submission or license decision.
          ignore: license
          comment: false
